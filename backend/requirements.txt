# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sse-starlette==2.1.3

# --- DB ---
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Queue ---
celery==5.4.0
redis==5.2.1

# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Storage (S3 / MinIO) ---
boto3==1.35.92

# --- Generation providers ---
openai>=1.50,<2
anthropic>=0.40,<1
google-genai>=0.3,<2
fal-client==0.5.6
elevenlabs==1.50.3

httpx==0.28.1

# --- Multi-agent storyboard/narration refine (pipeline/refine.py, lazy-imported) ---
crewai>=0.80,<1

# --- Dev / test ---
pytest==8.3.4

# --- Phase 4: audio analysis (beat grid). Runs for real even in mock mode. ---
librosa==0.10.2.post1
numpy==2.1.3
soundfile==0.12.1
